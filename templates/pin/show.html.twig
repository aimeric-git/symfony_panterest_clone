{% extends "base.html.twig" %}

{% block body %}

    <h1>Show Pin</h1>
    <article>
        <h1> {{ pin.title }} </h1>
        <p> {{ pin.description }}</p>
        <a href=" {{ path('app_index') }} ">Go Back</a>
    </article>
{% endblock %}